# CLAUDE.md

The working agreement for this repo lives in **[AGENT.md](AGENT.md)**.

Read it before changing anything. It covers the development workflow, the
package architecture and dependency rules, Go and SQLite conventions specific to
this codebase, the HTTP and frontend rules, and the TDD requirements.

Nothing is duplicated here on purpose — two copies of a rule is how one of them
ends up wrong.
